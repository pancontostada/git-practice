ok so there was a lot here. testing. one-sentence, i learned how to run tests on ui and user interactions using vitest. also did some accessability testing, learned about testing coverage, and the different types of tests which i found fascinating. it cleared up a lot of words ive encountered in work, like smoke_test. so theres a lot of tooling, it involves testing-library as a library to render things from react. vitest provides the program to run tests using functions like test, expect, and describe. testing-library handles screen and render, which are almost like the document.getElementById of testing. other buzzwords: .toBeInTheDocument(), toBe, .getByRole, .getByText, .getByTextContent(?), .getByAllRoles[0], AAA (arrange, act, assert), userEvent.setup(), user.type, user.clear(), user.click(), screen.getByRole('textbox'). .getByAltText, TDD (test-driven development, extreme programming), functional testing, non-functional testing (a11y, usability testing), unit testing, component testing, black box testing, gray box testing, white box testing, integration testing, regression testing (when you add a new feature and see if it breaks the app), smoke alarm (testing just the most critical features of your app), api/contract testing, external testing, oh yeah there was a section on external services testing that mocked ("mock testing") an api. that also involved a LOT of tooling. overall p cool stufff